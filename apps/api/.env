
# 开发环境
# 后端连接 MySQL 的地址。格式 mysql://用户名:密码@主机:端口/数据库。例如本地 MySQL 默认端口 3306，库名 turntable。
# DATABASE_URL=mysql://root:root@192.168.1.2:3306/turntable
# JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
# PORT=5000
# NODE_ENV=production
# CORS_ORIGINS=http://localhost:5173




# 生产环境
# 后端连接 MySQL 的地址。格式 mysql://用户名:密码@主机:端口/数据库。例如本地 MySQL 默认端口 3306，库名 turntable。
# 生产
DATABASE_URL=mysql://order-takeout-turntable:a123456@47.113.179.233:3306/order-takeout-turntable
# 用于签发/校验 JWT 的密钥。必须设置成足够随机的字符串。
JWT_SECRET=change_me_in_prod
# 允许跨域的前端来源列表（逗号分隔）。
CORS_ORIGINS=http://localhost:5173
# 后端 API 监听端口
PORT=5000
